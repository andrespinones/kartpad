# KartPad PPC/AArch64 semantics ledger

No PPC/AArch64 semantic result is claimed yet. The first deliverable is a no-game-data arm64/x86_64 differential harness covering integer, scalar floating-point, paired-single, conversion, estimate, flags, and ABI behavior.

## Input-oracle observation

Dolphin's named-pipe controller is proven for deterministic menu transitions. An exploratory Luigi Circuit drive did not disambiguate the apparent A/B race behavior, despite menu A behaving correctly, so acceleration/brake semantics are intentionally open. Before KartPad input acceptance, add a minimal fixture or frame-correlated trace that observes each button independently; do not encode the exploratory guess.
