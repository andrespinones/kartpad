# KartPad PPC/AArch64 semantics ledger

No semantic result is claimed yet. The first deliverable is a no-game-data arm64/x86_64 differential harness covering integer, scalar floating-point, paired-single, conversion, estimate, flags, and ABI behavior.
