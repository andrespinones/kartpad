# KartPad PPC/AArch64 semantics ledger

Updated: 2026-08-28

## Current gate state

G6 remains **In progress**. The portable no-game-data contract is green on arm64 and x86_64/Rosetta, but the full translator-emitted helper surface has not yet been routed through the portable implementation. A passing test-only subset is not the title runtime.

## Proven inventory

| Family | Coverage | Result |
|---|---|---|
| Integer | rotate/mask/shift, signed/unsigned divide edges, count-leading-zero, CR signed/unordered, add/sub carry/borrow/overflow, 100,000 seeded rotate/mask pairs | Pass on arm64 and x86_64 |
| Scalar FP | f32 rounding, signed zero, NI single-subnormal flush, all four float-to-word rounding modes, invalid/divide-by-zero/overflow/underflow/inexact flags, fused rounding point, 25-bit multiplier rounding | Pass on arm64 and x86_64 |
| Estimates | fres/frsqrte zeros, infinities, normal, subnormal, NaN payload, negative and boundary vectors | Bit-exact against compiled pinned Dolphin `FloatUtils.cpp` |
| Paired-single | add/sub/mul/div/fused madd/merge/select and 50,000 seeded finite randomized triplets | Identical raw-bit state hash on arm64 and x86_64 |
| GQR arithmetic | signed/unsigned clamp and wrapped-scale dequantization boundary samples | Pass |
| ABI/translation | mixed host-call ABI contract plus an actual pinned-translator DOL fixture executing integer add, checked-memory loads/stores and `fadds` | Pass on arm64 and x86_64 |
| Sanitizers | complete arm64 contract and translated fixture | ASan/UBSan Pass |

Both Release architectures report 250,080 checks and state hash `0xca5a9534a8da687b`. The translated fixture reports integer `65534`, `fadds` raw bits `0x40700000`, and checked-memory Pass.

## Oracle boundary

The estimate corpus is regenerated by compiling the locked Dolphin implementation directly, then compared byte-for-byte with `fixtures/g6/dolphin-estimates.txt`. No game data is used. Conservative flags are target-local: `-fno-fast-math -ffp-contract=off`.

## Resolved mismatches

- The first upward-rounding conversion returned 2 instead of 3 because optimized host `nearbyint` did not honor a dynamically changed environment at that call site. Guest rounding is now selected explicitly with `trunc`/`ceil`/`floor`/nearest, independent of ambient host optimization.
- Initial hand-entered `Force25Bit`, fres/frsqrte, and scale-vector expectations were wrong. The implementation was held fixed; expectations were corrected only after applying the upstream algorithm by inspection or compiling pinned Dolphin's oracle.
- Leak detection is unavailable in this macOS ASan runtime. The sanitizer run now disables only unsupported leak detection; ASan and UBSan remain enabled.

## Remaining G6 work

- Port the complete upstream ISA/helper surface away from x86 SSE/MXCSR assumptions, including full paired-single forms, quantized load/store for every GQR encoding, FPSCR/CR/XER updates, reservation helpers, conversions, estimates, and NI behavior.
- Prove every translator-emitted operation reaches that portable layer rather than the narrow fixture shim.
- Add translated fixtures for paired-single, GQR, FPSCR flags, LR/CTR/stack/varargs, and host callbacks.
- Keep game/ghost state-hash acceptance pending until a real game runtime exists; any later divergence reopens G6 immediately.

Run `./scripts/test-ppc-semantics.sh` for the current reproducible subset.
